# Sekans — kök .htaccess (public_html/)
# SPA geri dönüşü, /api ve /uploads'ı YUTMAZ; ayrıca cache + güvenlik başlıkları.

<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # 1) API ve uploads'ı SPA geri dönüşünden TAMAMEN hariç tut.
  #    ^api ve ^api/... ile ^uploads ve ^uploads/... her ikisi de eşleşir.
  RewriteRule ^api(/.*)?$ - [L]
  RewriteRule ^uploads(/.*)?$ - [L]

  # 2) index.html olduğu gibi.
  RewriteRule ^index\.html$ - [L]

  # 3) Gerçek dosya/dizin değilse SPA kabuğuna düş.
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>

# Hash'li varlıkları uzun cache'le; HTML kabuğunu cache'leme.
<IfModule mod_headers.c>
  <FilesMatch "\.(js|css|woff2?|png|jpe?g|svg|webp|gif)$">
    Header set Cache-Control "public, max-age=31536000, immutable"
  </FilesMatch>
  # index.html ASLA cache'lenmemeli.
  #
  # Varlık adları içeriğe göre hash'lidir (index-AbC123.js); hangi JS/CSS'in
  # yükleneceğini SADECE index.html söyler. Bu dosya cache'lenirse tarayıcı
  # sunucudaki yeni sürümden habersiz eski varlıkları istemeye devam eder ve
  # site "geç güncellenmiş" görünür — gizli pencere de bunu çözmez.
  # (Test sunucusunda tam olarak bu yaşandı; kural orada da eklendi.)
  <FilesMatch "index\.html$">
    Header set Cache-Control "no-store, must-revalidate"
  </FilesMatch>
  Header set X-Content-Type-Options "nosniff"
  Header set X-Frame-Options "SAMEORIGIN"
  Header set Referrer-Policy "strict-origin-when-cross-origin"
</IfModule>

<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/css application/javascript application/json image/svg+xml
</IfModule>
